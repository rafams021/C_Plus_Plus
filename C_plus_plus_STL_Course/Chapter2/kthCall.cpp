// Function that prints count every time is called
void utility() {

    // declare the variable count here
    int static count;
    // increases the value of count by 1
    count++;

    // print count
    cout << count;
}